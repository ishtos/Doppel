# App Icon Assets

Place the following files in this directory:

- `app_icon.png` (1024x1024) - Main app icon
- `app_icon_foreground.png` (1024x1024) - Android adaptive icon foreground (with padding)

After placing the files, run:
```bash
dart run flutter_launcher_icons
```
